[build]
  base = "Real-estate"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  CI = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
